import { TestBed } from '@angular/core/testing';

import { updnewpropertyService } from './updnewproperty.service';

describe('NewpropertyService', () => {
    let updnewpropertyService: updnewpropertyService;

    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [updnewpropertyService],
        });
        // updnewpropertyService = TestBed.inject(updnewpropertyService);
    });

});
